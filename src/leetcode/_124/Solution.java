package leetcode._124;

public class Solution {
    // in a -1,2,3 tree it is possible to include -1 also which is root
    // if 1,-2,3 we can also create path without 2 or 3 if it is negative
    // for a more rooted tree like 1,2,3,4,5 there is only one split we can do in
    // path if it is done we cant include its parent in the tree
}
